.container-fluid.d-flex.align-items-center.justify-content-center.text-center(style="height: 100vh;")
  .row
    .col
      h1(display-1.text-danger) 404 - Page Not Found
      p(lead.mb-4) The page you are looking for does not exist.
      button(
        onclick="history.back()"
      ) Trở lại